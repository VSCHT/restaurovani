.ui.modal {
  padding: 20px;
  background-color: @basicColor;

  & > .image.content {
    justify-content: space-between;
  }

  @media only screen and (max-width: @mobileSize) {
    & .image.content {
      flex-direction: row;
    }
  }
}

