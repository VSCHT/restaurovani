files:
  use:
  - invenio_files
  module:
    qualified: restoration
  resource:
    base-classes: [ uct_extras.theses.file_resource.S3RedirectFileResource ]
  properties:
    fileType:
      type: keyword
      enum:
        - photo
        - document
    featured:
      type: boolean
    caption:
      type: keyword

